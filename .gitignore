# Cross-cutting ignores for the monorepo (web/ and api/ each also have their
# own .gitignore for stack-specific paths — keep additions there, not here).

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Local tool state (not project source)
/.claude/
/.agents/
/.playwright-mcp/

# Docker
docker-compose.override.yml
